# Dependencies
node_modules/

# Build
dist/
src-tauri/target/

# IDE
.idea/
.vscode/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db

# Environment / credentials
*.env
.env.*
.env

# Local runtime data (never commit user secrets/history)
*.db
*.db-shm
*.db-wal
providers.json
config.ini
dictionary.txt
history.md
corrections_stats.json
failed_audio/
/themes/
*.log
logs/
debug/

# Test artifacts
*.tsbuildinfo
playwright-report/
test-results/

# Release build output (populated by scripts/build-all-platforms.sh; must never
# be committed — a stray `git add -A` would otherwise sweep 100+MB of binaries)
artifacts/
src-tauri/tests/screenshots/

# Claude / local agent tooling
.claude/
.superpowers/
.pi-subagents/
.pi/

# Local tool caches
.pytest_cache/
.ruff_cache/

# Python version (legacy, kept locally)
.python-version
pyproject.toml
uv.lock
src/soupawhisper/

# Keep benchmark scripts, ignore Python legacy scripts
scripts/*.py
.worktrees/

# Standalone embeddable visualizer package (own git repo at github.com/axelbaumlisto/metaballs-viz)
/metaballs-viz/

# Local-only artifacts (media exports, capture specs, screenshots, scratch notes)
docs/media/
docs/archive/root-notes/
e2e/_bz-didinium-capture.spec.ts
e2e/screenshots/
progress.md


# ado/session junk
*.pid
.tmp-*
.tmp-landing-shots/

# docs-screenshotter scratch (regenerated each CI run)
docs-site/_capture-screenshots.mjs

# Tauri-generated schemas/ACL (regenerated from src-tauri/capabilities/ on build)
src-tauri/gen/
